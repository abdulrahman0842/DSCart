import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color scaffoldBackground = Colors.white;

  // AppBar & BottomNav
  static const Color appBar = Color.fromARGB(255, 247, 225, 196);
  static const Color bottomNav = Color.fromARGB(255, 247, 225, 196);
  static const Color bottomNavIndicator = Color.fromARGB(255, 245, 200, 140); // slightly softer

  // Product price & primary accent
  static const Color price = Color(0xFF388E3C); // warm brown
  static const Color primary = Color(0xFF8D6E63);


  // Text
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.black54;

  // Status colors
  static const Color success = Colors.green;
  static const Color error = Colors.redAccent;
}
